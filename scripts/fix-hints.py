"""
Replace low-quality auto-generated strategy hints in problems.json with
topic- and section-aware hints that actually describe the algorithm.
"""
import json
import re
import sys

# ─── hint mapping ───────────────────────────────────────────────────────────
# Keys: (topic_id, section_key)  where section_key is a substring match
#       against study_plan_refs[0]['section_title'].
# Fallback keys: (topic_id,)
# Values: list[str] of 2-3 hints in Traditional Chinese.
#
# Each hint should tell:
#   1. WHAT to do (concrete algorithm / pattern)
#   2. WHY it works (key insight / invariant)
#   3. HOW to avoid common pitfalls

HINTS: dict[tuple, list[str]] = {

    # ── binary-search ────────────────────────────────────────────────────────
    ("binary-search", "基礎"): [
        "維護半開區間 [l, r)，循環不變式是答案在 [l, r) 內；命中條件成立時收 r = mid，否則 l = mid + 1。",
        "循環結束時 l == r，直接回傳 l 作為第一個合法位置，無需額外後處理。",
        "先問自己：這題等同於找第一個讓某單調 predicate 為 true 的位置嗎？確認後再套 lower_bound 框架。",
    ],
    ("binary-search", "最大化最小值"): [
        "問「最小值最大能到多少」⟺ 二分答案 x，再貪心判定能否讓所有最小值 ≥ x。",
        "check(x)：從左到右貪心放置，每次選最遠的合法位置，數量 ≤ 限制即可行。",
        "答案範圍初始化為 [min_gap, total_range]，注意 check 函式必須對 x 單調。",
    ],
    ("binary-search", "最小化最大值"): [
        "問「最大值最小能到多少」⟺ 二分答案 x，再貪心判定能否讓最大值 ≤ x。",
        "check(x)：貪心切分，只要當前累積值超過 x 就新開一組，統計組數 ≤ 限制即可行。",
        "二分範圍：下界是單元素最大值，上界是所有元素之和；check 的單調性決定能否二分。",
    ],
    ("binary-search", "第 K 小"): [
        "不要直接找第 K 個；二分答案 x，計算 ≤ x 的元素個數 cnt(x)，找最小的 x 使 cnt(x) ≥ K。",
        "cnt(x) 通常能用乘法表列式或排序後二分在 O(n) 或 O(n log n) 內算出。",
        "注意重複元素：cnt(x) 表示「嚴格 ≤ x 的個數」，確保 lower_bound 語意正確。",
    ],
    ("binary-search", "三分法"): [
        "三分法用於單峰函數求極值：取 m1 = l + (r-l)/3，m2 = r - (r-l)/3，比較 f(m1) 與 f(m2) 縮小範圍。",
        "整數域三分在 r - l < 3 時需線性枚舉剩餘點，避免死循環。",
        "確認函式是嚴格單峰（先遞增後遞減，或反之），否則三分法可能找不到全域極值。",
    ],
    ("binary-search", "進階"): [
        "先辨識『可行條件關於答案單調』這一前提，再確認 check(x) 能在 O(n) 或 O(n log n) 內算出。",
        "當直接最佳化很難，嘗試把目標改寫成判定題 check(x)，再在值域上二分。",
        "注意溢位：mid = l + (r - l) / 2，浮點二分用固定迭代次數（如 100 次）替代精度終止。",
    ],
    ("binary-search", "其他"): [
        "先辨識問題是否具有「答案越大越容易滿足（或越小越容易滿足）」的單調性。",
        "把求最優值改寫為二分判定函式 check(x)，再在合理值域上二分找臨界點。",
    ],
    ("binary-search", "求最大"): [
        "二分找最大的 x 使 check(x) 為真；初始化 l = min_feasible, r = max_candidate。",
        "命中時把 l 往右推（l = mid + 1 但保留 mid 到答案），不命中時 r = mid - 1。",
    ],
    ("binary-search", "求最小"): [
        "二分找最小的 x 使 check(x) 為真；命中時 r = mid（保留 mid），不命中時 l = mid + 1。",
        "初始化 l = 理論下界，r = 理論上界，終止時 l 即為最小可行值。",
    ],
    ("binary-search", "分間接值"): [
        "答案本身不是陣列元素，而是某個衍生量；先把衍生量的值域確定，再二分這個量。",
        "check(x) 計算在衍生量 ≤ x 的限制下，能否滿足原題目條件（如成本不超過預算）。",
    ],

    # ── graph-traversal ──────────────────────────────────────────────────────
    ("graph-traversal", "網格圖 DFS"): [
        "把每格 (r, c) 當節點，用方向陣列 dr/dc 枚舉上下左右鄰格，DFS 前先檢查越界與障礙。",
        "進入 DFS 時立即標記 visited（或直接修改格值），避免重複入隊。",
        "若需計算連通分量大小，在 DFS 結束時累計回傳。",
    ],
    ("graph-traversal", "網格圖 BFS"): [
        "多源 BFS：把所有起點同時入隊、距離設 0，等同接一個虛擬超源，之後正常 BFS。",
        "BFS 層數就是最短步數；入隊時立即標記 visited，避免同節點多次入隊。",
        "若是 0-1 BFS（邊權只有 0 或 1），改用 deque：0 邊 push_front，1 邊 push_back。",
    ],
    ("graph-traversal", "拓撲排序"): [
        "Kahn 演算法：統計入度，入度為 0 的節點先入隊，每彈出一個就把鄰居入度 -1，降到 0 再入隊。",
        "最後若入隊節點數 < V，代表圖中有環；拓撲排序的性質正是 DAG 特有的。",
        "在拓撲序上做 DP 時，每個節點出隊後直接更新後繼節點的狀態（推式 DP）。",
    ],
    ("graph-traversal", "基環樹"): [
        "基環樹 = 樹 + 一條多餘的邊，形成恰好一個環；先找出環，再以環上節點為根做樹形 DP。",
        "找環：DFS 記錄 in_stack，遇到已在棧中的節點即找到環的起點，回溯記錄環上所有節點。",
        "環上節點的相鄰樹枝可先做普通樹形 DP，最後枚舉環的起點（斷開一條環邊）合併兩次結果。",
    ],
    ("graph-traversal", "二分圖"): [
        "BFS/DFS 染色：用 0/1 交替標記鄰節點，若發現同色衝突則不是二分圖。",
        "等價判斷：無奇環 ⟺ 二分圖；有向圖判二分圖需考慮強連通分量再染色。",
    ],
    ("graph-traversal", "強連通分量"): [
        "Tarjan 一次 DFS 求 SCC：維護 disc（發現時間）與 low（子樹可回到的最早祖先時間），根節點彈出一整個 SCC。",
        "縮點後形成 DAG，在 DAG 上做 DP 或貪心往往是後續步驟。",
        "low 只從 back edge（仍在 stack 中的節點）更新，不從 cross edge 更新。",
    ],
    ("graph-traversal", "雙連通分量"): [
        "橋（Bridge）：DFS 後若 low[v] > disc[u]，則邊 (u,v) 是橋。",
        "割點（Articulation Point）：存在子節點 v 使 low[v] >= disc[u] 時，u 是割點（根節點需 ≥ 2 子樹）。",
        "邊雙連通分量縮點：先找橋再 DFS 找各連通塊，每塊為一個邊雙連通分量。",
    ],
    ("graph-traversal", "在拓撲序上 DP"): [
        "先做拓撲排序，按拓撲序枚舉每個節點，轉移只能從已確定節點推向後繼。",
        "確認 DP 定義的語意（如最長路徑、最大收益），在每個節點出隊後更新其後繼的狀態。",
    ],
    ("graph-traversal", "圖 DP"): [
        "把圖上的最優決策拆成節點狀態，由 BFS/DFS 順序推進，類似記憶化搜尋。",
        "若圖有環，需先確認 DP 仍合法（如只在 DAG 上），否則改用 Bellman-Ford 或環處理。",
    ],
    ("graph-traversal", "跳躍遊戲"): [
        "把跳躍位置建成圖，BFS 計算從起點最少跳幾次能到達終點。",
        "關鍵在於如何壓縮無效跳點：預先排序或用並查集合併可達的連續區間。",
    ],
    ("graph-traversal", "尤拉路徑"): [
        "尤拉路徑存在條件：無向圖中恰好 0 或 2 個節點的度為奇數；有向圖中至多一個節點出度 = 入度 + 1，至多一個入度 = 出度 + 1。",
        "Hierholzer 演算法：從起點 DFS，退出遞迴時把節點加入路徑，最後逆轉即為答案。",
    ],
    ("graph-traversal", "BFS"): [
        "BFS 在無權圖中保證最短路：按層擴展，每個節點第一次出隊時距離已是最短。",
        "入隊時立即標記 visited（而非出隊時），防止同一節點被多次加入隊列。",
    ],
    ("graph-traversal", "DFS"): [
        "DFS 用於遍歷、連通分量、括號匹配、或尋找路徑是否存在。",
        "進入函式時標記 visited，回溯時視需要取消標記（回溯法）。",
    ],

    # ── shortest-path ────────────────────────────────────────────────────────
    ("shortest-path", "Dijkstra"): [
        "最小堆存 (dist, node)，每次 pop 最小距離者做鬆弛；用懶刪除跳過過期條目（比對 d != dist[u]）。",
        "鬆弛條件：dist[v] > dist[u] + w 時才更新並推入堆，確保每條最短路只被確認一次。",
        "邊權必須非負才能用 Dijkstra；有負邊需改用 Bellman-Ford 或 SPFA。",
    ],
    ("shortest-path", "0-1 BFS"): [
        "邊權只有 0 或 1 時，用雙端佇列替代堆：0 邊 push_front，1 邊 push_back，保持佇列按距離單調。",
        "複雜度 O(V+E)，比 Dijkstra 少一個 log 因子；判斷能否用 0-1 BFS：邊權集合是否只有兩種值。",
    ],
    ("shortest-path", "Bellman-Ford"): [
        "對所有邊鬆弛 V-1 輪，第 k 輪結束後保證最多經過 k 條邊的最短路正確；第 V 輪若仍能鬆弛則有負環。",
        "SPFA 是 Bellman-Ford 的佇列優化，平均快但最壞仍 O(VE)；競賽中可能被卡，優先用 Dijkstra。",
    ],
    ("shortest-path", "Floyd"): [
        "Floyd-Warshall 三重迴圈求所有對最短路：dp[k][i][j] 表示只經過節點 1..k 的 i→j 最短路。",
        "迴圈順序必須是最外層 k、然後 i 和 j；初始化 dp[i][i] = 0，其餘設 INF（注意避免加法溢位）。",
    ],
    ("shortest-path", "最短路"): [
        "先判斷邊權特性：非負用 Dijkstra，有負邊用 Bellman-Ford，只有 0/1 用 deque BFS，全對全用 Floyd。",
        "建圖方式（鄰接表/矩陣/隱式圖）決定枚舉鄰居的效率，確認節點與邊的規模再選擇。",
    ],
    ("shortest-path",): [
        "先判斷邊權特性：非負用 Dijkstra；含負邊用 Bellman-Ford；邊權 0/1 用 deque BFS；全對全用 Floyd。",
        "建圖時確認邊是有向還是無向，避免遺漏反向邊導致連通性錯誤。",
    ],

    # ── dp-fundamentals ──────────────────────────────────────────────────────
    ("dp-fundamentals", "0-1 背包"): [
        "壓成一維 dp[c]，外層枚舉物品、內層逆序枚舉容量（c 從大到小），確保每件物品只選一次。",
        "轉移：dp[c] = max(dp[c], dp[c - w] + v)；逆序的理由是防止同一輪剛更新的 dp[c-w] 被重複使用。",
    ],
    ("dp-fundamentals", "完全背包"): [
        "與 0/1 背包幾乎相同，差別在容量改為正序枚舉（c 從小到大），允許同一物品被多次選取。",
        "dp[c] = max(dp[c], dp[c - w] + v)，正序保證 dp[c - w] 已含本物品，支援重複選。",
    ],
    ("dp-fundamentals", "分組背包"): [
        "每組只能選一個：外層枚舉組，中層逆序枚舉容量，最內層枚舉同組內哪個物品。",
        "注意：不能在同一組物品上連續更新同一容量，否則相當於從同組選了多個。",
    ],
    ("dp-fundamentals", "LIS"): [
        "O(n log n) 版本：維護 tails 陣列，tails[k] 是長度 k+1 的遞增子序列最小結尾；對每個新元素用 lower_bound 找插入位置並替換。",
        "嚴格遞增用 lower_bound，非嚴格遞增改用 upper_bound；tails 長度即為 LIS 長度。",
    ],
    ("dp-fundamentals", "狀態機"): [
        "定義各個狀態（如持股/不持股/冷卻）及每種操作對應的轉移方程，確保狀態之間的轉移覆蓋所有情況。",
        "畫出狀態轉移圖：節點是狀態，邊是操作；確認每條邊的條件與代價後再寫程式碼。",
    ],
    ("dp-fundamentals", "數位 DP"): [
        "記憶化搜尋，狀態通常包括（當前位、是否仍緊貼上界 tight、是否已有前導零 started、其他計數）。",
        "tight 控制當前位最大可填多少：tight 時最多填 digit[pos]，否則可填 0-9。",
        "注意前導零對計數的影響，start 標誌位為 false 時跳過某些統計。",
    ],
    ("dp-fundamentals", "博弈"): [
        "博弈 DP：dp[state] = 目前先手是否必勝；若存在任何讓後手必輸的後繼狀態，先手必勝。",
        "Nim 遊戲：所有堆數的 XOR 為 0 時先手必輸；Sprague-Grundy 定理把複合遊戲拆分成獨立子遊戲求 Grundy 值。",
    ],
    ("dp-fundamentals", "區間 DP"): [
        "dp[l][r] 由子區間 dp[l][k] 和 dp[k+1][r] 合併而來；枚舉順序必須由小區間到大區間（外層枚舉長度）。",
        "分割點 k 枚舉 l ≤ k < r（注意左閉右開與閉區間的差異）；注意 k 是分割點還是端點的語意。",
    ],
    ("dp-fundamentals", "前字尾分解"): [
        "把陣列分成前段 [0, i) 與後段 [i, n)，預先計算前綴 dp[] 和後綴 dp[]，再枚舉分割點 i 合併。",
        "常見於「中間某點」或「拆成兩半」類型；注意兩段的邊界包含關係（閉還是開）。",
    ],
    ("dp-fundamentals", "把 X 變成 Y"): [
        "定義操作的代價，dp[i][j] 表示從狀態 i 轉成狀態 j 的最小代價；類似字串編輯距離的框架。",
        "若操作集合固定，先確認每種操作改變了哪個維度，再設計轉移。",
    ],
    ("dp-fundamentals", "跳躍遊戲"): [
        "dp[i] = 從起點到位置 i 的最優結果（最少跳數、最遠位置、是否可達）。",
        "貪心跳躍：每步把目前能到達的最遠位置更新為 max_reach；需要新一跳時才計數。",
    ],
    ("dp-fundamentals", "輸出具體方案"): [
        "在 DP 填表時同時記錄 from[] 或 choice[]，記錄每個狀態從哪個前驅轉移而來。",
        "最後從終點沿著 from[] 回溯，逆序輸出路徑；注意去重（若有多條最優路徑只需輸出一條）。",
    ],
    ("dp-fundamentals", "機率"): [
        "期望 DP：E[state] 從後往前算，E[state] = Σ p_i × (cost_i + E[next_state_i])。",
        "注意期望的線性性：整體期望 = 各獨立部分期望之和；若各步驟不獨立，需帶入完整狀態。",
    ],
    ("dp-fundamentals", "劃分型貪心"): [
        "能用貪心的划分問題，通常具有「每段越短越好」或「均勻劃分最優」的單調性。",
        "驗證貪心策略：若交換相鄰劃分點不改善答案，貪心成立；否則需用 DP 枚舉分割點。",
    ],
    ("dp-fundamentals",): [
        "先定義狀態語意（dp[i] 或 dp[i][j] 代表什麼），確保無歧義後再推導轉移方程。",
        "確認枚舉順序：dp[x] 依賴的所有前驅狀態都必須已計算完畢，否則調整迴圈方向。",
    ],

    # ── greedy ───────────────────────────────────────────────────────────────
    ("greedy", "從最小/最大開始貪心"): [
        "貪心策略：優先處理代價最小（或最大）的選項，為後續操作留下最大彈性。",
        "驗證貪心的正確性：若交換任意相鄰決策後結果不改善，說明局部最優即全域最優。",
    ],
    ("greedy", "從最左/最右開始貪心"): [
        "從一端開始，每次選擇在目前約束下最佳的局部決策，推進到另一端。",
        "常見於區間排程（按結束時間從小到大貪心）或字串匹配（從左到右掃描）。",
    ],
    ("greedy", "反悔貪心"): [
        "先做貪心決策，若後來發現之前的選擇不優，把它放回堆中替換——用堆模擬撤銷操作。",
        "狀態：最大堆存「目前選中且代價最大的項目」，遇到更優替換時彈出舊的推入新的。",
    ],
    ("greedy", "中位數貪心"): [
        "把所有元素調整到某個目標值的最小總代價，最優目標通常是中位數（最小化 L1 距離）。",
        "若需調整到平均值（最小化 L2 距離），改用均值；先排序後從中位數位置推導即可。",
    ],
    ("greedy", "均值不等式"): [
        "若乘積固定求和最小（或和固定求乘積最大），均值不等式說明各項相等時最優。",
        "把問題轉化為「固定某量最大化另一量」，再用 AM-GM 或排序不等式論證貪心策略。",
    ],
    ("greedy", "兩次掃描"): [
        "從左掃一遍維護前綴資訊，再從右掃一遍維護後綴資訊，最後合併兩端結果。",
        "適合「每個位置的答案依賴左側和右側各自最優」的問題。",
    ],
    ("greedy", "先列舉，再貪心"): [
        "先枚舉某個關鍵參數（如分割點、中間元素），固定後對剩餘部分做貪心決策。",
        "枚舉的對象通常使問題分解成獨立子問題，每個子問題再用 O(1) 或 O(log n) 貪心求解。",
    ],
    ("greedy", "乘積貪心"): [
        "涉及乘積時，注意負數的影響：兩個負數相乘為正；把正負數分開排序後合併。",
        "若要最大化乘積，考慮把最大的正數和最小的負數（絕對值最大）配對。",
    ],
    ("greedy", "單序列配對"): [
        "把兩個序列排序後逐一對應，通常是相同順序配對（最大配最大）或反序配對（最大配最小）。",
        "用排序不等式驗證：同序配對最大化乘積之和，反序最小化。",
    ],
    ("greedy", "分類討論"): [
        "把所有情況分類，每種情況下獨立證明貪心策略的正確性，最後合併所有情況的答案。",
        "注意邊界條件：單個元素、空集合、所有元素相同等極端情況。",
    ],
    ("greedy", "從特殊到一般"): [
        "先解決特殊情況（如 n=1、n=2），觀察規律後推廣到一般情況。",
        "常見技巧：歸納法證明貪心正確性，或對排序後的序列歸納。",
    ],
    ("greedy",): [
        "貪心策略：每次選擇當前最優的局部決策，期望能得到全域最優。",
        "驗證方法：交換論證（若交換相鄰決策不改善結果）或歸納法確認貪心有效。",
    ],

    # ── two-pointers ─────────────────────────────────────────────────────────
    ("two-pointers", "基礎"): [
        "滑動視窗：右端擴張加入元素，視窗不合法時持續縮左端，每個元素最多各進出一次，總時間 O(n)。",
        "使用前提：right 右移時，「最小合法 left」單調不減；若含負數且條件非單調，雙指針可能失效。",
    ],
    ("two-pointers", "滑動視窗"): [
        "右端擴張加入元素，視窗不合法時持續縮左端，每個元素最多各進出一次，總時間 O(n)。",
        "最長合法視窗：先收縮到合法後更新答案；最短滿足視窗：合法後先更新再盡量收縮。",
    ],
    ("two-pointers", "對向雙指針"): [
        "排序後從兩端往中間夾逼；若 sum < target 移動左指針（增大），若 sum > target 移動右指針（減小）。",
        "找到答案後跳過相同值去重；對向雙指針依賴排序，若原始下標重要需一起保存。",
    ],
    ("two-pointers", "恰好 K"): [
        "「恰好 K 個」計數困難，改用 exactly(K) = atMost(K) - atMost(K-1) 的差分技巧。",
        "「至多 K 個」的滑動視窗更容易寫：右端加入，不滿足時縮左，每個右端位置加上 (right - left + 1)。",
    ],
    ("two-pointers", "快慢指針"): [
        "快指針每步走兩格、慢指針走一格；若有環，快慢指針必定在環上相遇（Floyd 判環）。",
        "找環入口：相遇後把一個指針移到頭，兩者同步走，再次相遇處即為環入口。",
    ],
    ("two-pointers",): [
        "雙指針依賴視窗或排序的單調性，每個指針只向一個方向移動，使 O(n²) 降到 O(n)。",
        "先確認前提：滑動視窗需要視窗合法性關於 left 單調；對向雙指針需要陣列有序。",
    ],

    # ── math-number-theory ───────────────────────────────────────────────────
    ("math-number-theory", "判斷質數"): [
        "試除到 √n：對每個 2 ≤ i ≤ √n 檢查 n % i == 0，O(√n)；大量質數判斷用線性篩。",
        "線性篩（Euler sieve）：每個合數只被最小質因數篩一次，O(n) 篩出 1..n 中所有質數。",
    ],
    ("math-number-theory", "GCD"): [
        "gcd(a, b) = gcd(b, a % b)（歐幾里得演算法），注意 gcd(0, x) = x；lcm(a, b) = a / gcd(a, b) * b 避免溢位。",
        "許多整除性問題可轉化為 GCD 條件：若 a, b 都是 g 的倍數，則它們的任意線性組合也是。",
    ],
    ("math-number-theory", "因子"): [
        "找 n 的所有因子：枚舉 1..√n，若 i 整除 n，則 n/i 也是因子，O(√n)。",
        "因數個數估計：對 n ≤ 10^6，因子個數通常 ≤ 240；可依此判斷枚舉是否合法。",
    ],
    ("math-number-theory", "同餘"): [
        "取模的基本性質：(a + b) % m = ((a % m) + (b % m)) % m；注意負數取模要加 m。",
        "乘法逆元：若 m 是質數，a 的逆元 = a^(m-2) mod m（費馬小定理）；否則用擴展歐幾里得。",
    ],
    ("math-number-theory", "組合"): [
        "組合數 C(n, k) = C(n-1, k-1) + C(n-1, k)，可用 Pascal 三角或預計算階乘/逆元得 O(1) 查詢。",
        "注意 k > n 時 C(n, k) = 0；mod 質數時用 Lucas 定理處理大數組合。",
    ],
    ("math-number-theory", "容斥原理"): [
        "集合 A1∪A2∪…∪Ak 的元素數 = Σ|Ai| - Σ|Ai∩Aj| + … ±|A1∩…∩Ak|；用於計算「至少滿足一個條件」的數量。",
        "對偶：「至少一個」= 全集 - 「一個都沒有」；後者通常更容易計算。",
    ],
    ("math-number-theory", "博弈"): [
        "Nim 遊戲：所有堆的 XOR 為 0 時先手必輸，否則先手必勝；操作後使 XOR = 0 即為最優策略。",
        "Sprague-Grundy：每個遊戲的 Grundy 值 = mex（後繼狀態 Grundy 值的集合），複合遊戲取 XOR。",
    ],
    ("math-number-theory", "快速冪"): [
        "反覆平方法：a^n = a^(n/2)^2 × (n 為奇數時再乘 a)，O(log n)；用於大冪次或矩陣快速冪。",
        "矩陣快速冪可加速線性遞推（如斐波那契），把遞推轉成矩陣乘法再快速冪。",
    ],
    ("math-number-theory", "摩爾投票"): [
        "Boyer-Moore 投票：count 計數候選人，遇到不同的就相消，count 降到 0 換候選人。",
        "最後得到的候選人不一定是眾數，需再掃描一次確認其出現次數 > n/2。",
    ],
    ("math-number-theory", "數論分塊"): [
        "對 floor(n/i) 的值域分塊：相同值的 i 合成一段 [l, r]，下一段從 r+1 開始，共 O(√n) 塊。",
        "每塊可以 O(1) 計算和，使 Σ f(floor(n/i)) 從 O(n) 降到 O(√n)。",
    ],
    ("math-number-theory",): [
        "先識別問題涉及的數論結構（整除、質數分解、GCD、同餘），再選擇對應工具。",
        "大量計算時預處理質數篩、逆元表或組合數表可把單次 O(√n) 降到 O(1)。",
    ],

    # ── intervals ────────────────────────────────────────────────────────────
    ("intervals", "合併區間"): [
        "先按左端排序，掃描時維護當前段的右界 cur_end；若新區間左端 > cur_end 則開新段，否則延伸右界。",
        "只需按 start 排序，排序後相鄰區間的關係只有「不重疊」或「可合併」，一次掃描 O(n)。",
    ],
    ("intervals", "不相交區間"): [
        "按結束時間排序，貪心選結束最早且與已選不重疊的區間；每次選 end 最小的，為後面留下最大空間。",
        "等價做法：刪最少區間使剩餘不重疊 = 總區間數 - 可保留的最多不重疊區間數。",
    ],
    ("intervals", "區間分組"): [
        "把所有區間拆成「開始」和「結束」事件，按時間排序掃描，用最小堆維護進行中的區間數量。",
        "最大同時重疊數 = 所需最少分組（如會議室）數；開始事件比同時刻結束事件晚處理。",
    ],
    ("intervals", "區間覆蓋"): [
        "按左端排序，在已覆蓋右界的前提下，每次貪心選右端最遠且左端 ≤ cur_end 的區間延伸覆蓋範圍。",
        "若排序後出現左端 > 目標右界的區間，說明有空隙，無法完整覆蓋。",
    ],
    ("intervals", "區間選點"): [
        "按右端排序，在每個右端位置放一個點，跳過左端 ≤ 當前點的所有後續區間（它們已被覆蓋）。",
        "等價於「最少不重疊區間數」，都是按 end 貪心。",
    ],
    ("intervals", "字首和"): [
        "prefix[i] = 前 i 個元素的和；區間 [l, r] 的和 = prefix[r+1] - prefix[l]，O(1) 查詢。",
        "注意下標偏移：prefix[0] = 0，prefix[i] 表示前 i 個；雜湊表配合前綴和可快速統計特定差值。",
    ],
    ("intervals", "二維字首和"): [
        "prefix[i][j] = 左上角矩形 [0..i-1][0..j-1] 的和；子矩形 [r1,c1]..[r2,c2] = prefix[r2+1][c2+1] - prefix[r1][c2+1] - prefix[r2+1][c1] + prefix[r1][c1]。",
        "建表時 prefix[i][j] = a[i][j] + prefix[i-1][j] + prefix[i][j-1] - prefix[i-1][j-1]（容斥）。",
    ],
    ("intervals", "差分"): [
        "差分陣列：diff[l] += v, diff[r+1] -= v；前綴和還原後即為每個位置的累加值。",
        "陣列要開到 n+2 防止 r+1 越界；適合批次修改後一次性查詢，若修改與查詢交錯需改用線段樹。",
    ],
    ("intervals", "計數排序"): [
        "元素值域小且為整數時，直接建桶計數再掃描，O(n + V) 優於比較排序的 O(n log n)。",
        "基數排序從低位到高位對穩定計數排序做多輪，適合值域大但位數少（如 32 位整數 × 4 輪）。",
    ],
    ("intervals", "桶排序"): [
        "把元素按值域均勻分配到 k 個桶，桶內排序後拼接，平均 O(n)。",
        "適合資料分布均勻的情況；若分布不均，某個桶可能退化到 O(n²)。",
    ],
    ("intervals", "列舉右，維護左"): [
        "固定右端 r，用資料結構快速查詢滿足條件的最優左端 l；通常維護的是「以某值結尾的最優狀態」。",
        "常用資料結構：雜湊表（O(1) 查特定差值）、單調棧（最近更大/更小）、BIT（前綴最值）。",
    ],
    ("intervals", "列舉中間"): [
        "固定中間位置 m，左側和右側獨立計算，最後合併；適合問題具有「中心對稱」或「分治」結構。",
    ],
    ("intervals",): [
        "區間問題通常先按左端（或右端）排序，再用貪心掃描或掃描線維護活躍集合。",
        "差分陣列適合批次靜態修改；若修改與查詢交錯，改用線段樹或 BIT。",
    ],

    # ── heap-priority-queue ──────────────────────────────────────────────────
    ("heap-priority-queue", "基礎"): [
        "堆支援 O(log n) 插入和取極值；C++ `priority_queue` 預設大根堆，小根堆需加 `greater<T>`。",
        "動態取出最小/最大時優先用堆，不需每次重新排序；取頂 O(1)，push/pop O(log n)。",
    ],
    ("heap-priority-queue", "懶刪除"): [
        "priority_queue 不支援刪任意元素；改 push 新版本，pop 時比對版本號（或 dist 值）丟棄過期條目。",
        "懶刪除的正確性依賴：每次取出的最小值只要仍是最新版本就展開，過期的直接跳過。",
    ],
    ("heap-priority-queue", "第 K 小/大"): [
        "固定大小為 K 的最大堆（大根堆）存前 K 小元素：若新元素 < 堆頂則替換堆頂，堆頂即第 K 小。",
        "若要第 K 大，用大小 K 的最小堆；維持堆大小始終為 K，複雜度 O(n log K)。",
    ],
    ("heap-priority-queue", "對頂堆"): [
        "對頂堆（大根堆 + 小根堆）維護動態中位數：大根堆存前一半，小根堆存後一半，兩堆大小差 ≤ 1。",
        "插入後調整：若新元素 ≤ 大根堆頂，推入大根堆，否則推入小根堆；再平衡兩堆大小。",
    ],
    ("heap-priority-queue", "鄰項消除"): [
        "把相鄰可消除的對子入堆（或棧），每次取最優對消除並更新其新的相鄰對，直到無法消除。",
        "注意消除後兩端相鄰元素可能形成新對，需要重新加入候選集合。",
    ],
    ("heap-priority-queue", "合法括號"): [
        "用棧匹配左右括號：遇左括號入棧，遇右括號若棧頂匹配則彈出，否則不合法。",
        "若允許刪除某些括號使合法，貪心先刪多餘右括號（從左掃），再刪多餘左括號（從右掃）。",
    ],
    ("heap-priority-queue", "莫隊"): [
        "莫隊演算法：離線處理區間查詢，按奇偶塊排序後移動左右端點，每次移動加入或移除一個元素。",
        "排序方式：左端點按塊號、塊號相同時奇數塊右端點升序/偶數塊降序（降低總移動次數）。",
    ],
    ("heap-priority-queue", "根號分解"): [
        "把 n 個元素分成 √n 個塊，每塊大小 √n；在塊級別做預處理，使查詢和更新都在 O(√n) 完成。",
        "塊內維護一個有序陣列或統計值，跨塊操作遍歷完整塊，端點塊暴力處理。",
    ],
    ("heap-priority-queue", "表示式解析"): [
        "遞迴下降解析：從頂層規則到底層操作數，利用運算符優先順序和結合律設計遞推函式。",
        "雙棧法（操作數棧 + 運算符棧）：遇運算符時彈出更高優先級的舊運算符先計算。",
    ],
    ("heap-priority-queue",): [
        "堆適合反覆取出當前最優候選的場景；注意 C++ `priority_queue` 預設大根堆。",
        "需要刪除特定元素時用懶刪除：push 新版本，pop 時丟棄舊版本。",
    ],

    # ── backtracking ─────────────────────────────────────────────────────────
    ("backtracking", "組合型"): [
        "組合型回溯：從 start 開始選元素，每次選 i 後遞迴從 i+1 開始，避免重複選同一元素。",
        "剪枝：若剩餘元素不夠填滿需求，提前 return（`n - i + 1 < k - path.size()`）。",
    ],
    ("backtracking", "子集型"): [
        "子集型回溯：對每個元素選或不選（二叉樹），或依序枚舉起點（不重複遍歷）。",
        "有重複元素時：排序後跳過同層的重複選擇（`if (i > start && a[i] == a[i-1]) continue`）。",
    ],
    ("backtracking", "排列型"): [
        "排列型回溯：用 used[] 標記已選元素，每次選擇尚未使用的元素。",
        "有重複元素時：排序後只在同層第一次出現時選擇（`if (i > 0 && a[i] == a[i-1] && !used[i-1]) continue`）。",
    ],
    ("backtracking", "劃分型"): [
        "劃分型回溯：從當前位置切一段，遞迴處理剩餘部分；剪枝條件為切段無效（長度不合法、不回文等）。",
        "選前看後：嘗試從 start 到 end 的每一段，若合法才遞迴，回溯後繼續下一個 end。",
    ],
    ("backtracking", "搜尋"): [
        "DFS 搜尋：狀態 = 當前位置，動作 = 移動方向或下一步選擇，visited 避免重複。",
        "BFS 適合求最短解；DFS 加剪枝（上界剪枝、可行性剪枝）適合求所有解或任意一解。",
    ],
    ("backtracking", "折半列舉"): [
        "Meet-in-the-Middle：把元素分兩半分別枚舉（各 2^(n/2)），排序後用二分或雙指針合併兩半結果。",
        "適合 n ≤ 40 的問題，把 2^n 降到 O(2^(n/2) · n)。",
    ],
    ("backtracking", "BFS"): [
        "狀態 BFS：把問題的狀態（如棋盤、配置）作為節點，合法轉移作為邊，BFS 找最短轉移步數。",
        "狀態去重很重要：用 unordered_set 或 visited 陣列記錄已擴展的狀態。",
    ],
    ("backtracking",): [
        "回溯框架：做選擇 → 遞迴 → 撤銷選擇；終止條件是達到目標長度或枚舉完所有元素。",
        "關鍵剪枝：提前判斷當前路徑不可能產生合法答案，直接 return。",
    ],

    # ── bitmask-dp ───────────────────────────────────────────────────────────
    ("bitmask-dp", "子集狀壓"): [
        "dp[mask] 表示選中 mask 中的元素後的最優值；枚舉 mask 的每個子集用 `for (sub = mask; sub; sub = (sub-1) & mask)`。",
        "SOS DP（Sum over Subsets）：dp[mask] = Σ dp[sub] for sub ⊆ mask，O(n × 2^n) 預處理。",
    ],
    ("bitmask-dp", "排列型狀壓"): [
        "dp[mask] = 對 mask 集合的元素做某種排列的最優值；轉移：從 dp[mask ^ (1<<last)] 推來。",
        "每次選 mask 中一個未選的位置 i，dp[mask] = min(dp[mask ^ (1<<i)] + cost(prev, i))。",
    ],
    ("bitmask-dp", "異或"): [
        "利用異或的性質：a ⊕ a = 0，a ⊕ 0 = a；前綴異或 prefix[i] = a[0] ⊕ ... ⊕ a[i]。",
        "子陣列異或 = prefix[r] ⊕ prefix[l-1]；用字典樹（Trie）可在 O(n log V) 找最大異或對。",
    ],
    ("bitmask-dp", "線性基"): [
        "線性基（Linear Basis）是一組元素，能表示原集合所有 XOR 可能值；插入時用高斯消元維護。",
        "最大異或值：從最高位到最低位貪心，若當前位能使結果更大則選該基向量。",
    ],
    ("bitmask-dp", "SOS"): [
        "SOS DP（Sum over Subsets）：dp[mask][i] = 考慮前 i 位，可以翻轉 mask 第 i 位得到的所有子集之和。",
        "轉移：`dp[mask][i] = dp[mask][i-1] + (mask>>i&1 ? dp[mask^(1<<i)][i-1] : 0)`，O(n · 2^n)。",
    ],
    ("bitmask-dp", "LogTrick"): [
        "AND/OR LogTrick：對每個位置 r，枚舉左端點 l，AND/OR 的值只會在 O(log V) 次後不再改變，可以壓縮。",
        "維護「目前所有不同 AND/OR 值」的列表，每次右端右移時與 a[r] 做運算，去重後更新。",
    ],
    ("bitmask-dp",): [
        "用整數的每一個 bit 表示一個元素是否選中，2^n 個狀態對 n ≤ 20 是可接受的。",
        "枚舉子集：`for (sub = mask; sub; sub = (sub - 1) & mask)` 枚舉 mask 的所有非空子集。",
    ],

    # ── monotonic-structure ──────────────────────────────────────────────────
    ("monotonic-structure", "基礎"): [
        "單調棧：從左到右掃描，棧中保持元素單調遞增（或遞減）；遇到破壞單調性的元素，彈出並處理。",
        "常用於「找左/右側第一個更大/更小元素」：每次彈出時，當前元素就是被彈出元素的下一個更大值。",
    ],
    ("monotonic-structure", "單調佇列"): [
        "固定長度視窗的最大/最小值：deque 存索引，保持對應值單調遞減（最大）或遞增（最小）。",
        "每步先移除過期索引（若 deque 頭 ≤ i - k），再從尾移除被當前元素支配的索引，最後 deque 頭即答案。",
    ],
    ("monotonic-structure", "貢獻法"): [
        "不從每個查詢的角度計算，改為計算每個元素對所有查詢的貢獻之和。",
        "單調棧確定每個元素的「作用範圍」（左右邊界），乘以係數後累加即為總貢獻。",
    ],
    ("monotonic-structure", "矩形"): [
        "柱狀圖最大矩形：用單調棧記錄「比當前高度矮的位置」，遇到下降時彈出並計算以彈出柱為高的最大矩形。",
        "二維矩陣中最大全 1 矩形：逐行累積高度，對每行的高度陣列做柱狀圖最大矩形。",
    ],
    ("monotonic-structure", "最小字典序"): [
        "貪心選字典序最小：從左掃，若棧頂元素 > 當前元素且後面還有棧頂元素，彈出棧頂；當前元素入棧。",
        "限制移除次數時，先計算每個字元可被移除的次數（出現次數 - 1），再做上述貪心。",
    ],
    ("monotonic-structure", "字典序"): [
        "單調棧維護字典序：從左到右掃，遇到比棧頂小的元素且剩餘有多個可移除機會，彈出較大元素。",
        "確認選取數量的約束，最後若棧中元素過多，截取前 k 個。",
    ],
    ("monotonic-structure", "單調佇列優化 DP"): [
        "當 dp[i] = min/max(dp[j] + cost(j, i)) 且 cost 可拆成 f(j) + g(i) 時，用單調佇列優化。",
        "固定 i，j 在一個視窗範圍內，單調佇列維護視窗內最優的 j，使轉移從 O(n) 降到 O(1)。",
    ],
    ("monotonic-structure",): [
        "單調棧核心操作：遇到不符合單調性的元素時，彈出並記錄（這是解答）；再推入當前元素。",
        "先想清楚棧中保存什麼（索引？值？）以及「待定」的語意是什麼（等待右側更大元素？）。",
    ],

    # ── linked-list ──────────────────────────────────────────────────────────
    ("linked-list", "前後指針"): [
        "用兩個指針，前者超前 k 步；當前者到末尾時，後者指向目標位置（如倒數第 k 個）。",
        "注意邊界：若需要刪除節點，後者應停在目標節點的前一個；使用 dummy head 避免刪除頭節點的特判。",
    ],
    ("linked-list", "快慢指針"): [
        "快指針每次走 2 步，慢指針走 1 步；快指針到末尾時，慢指針在中點（奇數長度偏左或偏右視初始化）。",
        "找環：兩者相遇後把一個移到頭，同步走，再次相遇即環入口。",
    ],
    ("linked-list", "反轉鏈表"): [
        "迭代反轉：prev = null, curr = head，每步 next = curr.next, curr.next = prev, prev = curr, curr = next。",
        "反轉子鏈：找到子鏈的前驅和後繼節點，反轉子鏈後重新連接。",
    ],
    ("linked-list", "合併鏈表"): [
        "雙指針合併兩個有序鏈表：用 dummy head，每次取較小節點接到結果鏈，直到一方走完後接剩餘。",
        "合併 K 個有序鏈表：優先佇列（最小堆）存各鏈表當前頭節點，每次 pop 最小後推入其 next。",
    ],
    ("linked-list", "刪除節點"): [
        "刪除節點時，只需改前驅節點的 next 指標；用 dummy head 避免刪除頭節點的特殊情況。",
        "若只給了要刪除的節點（無法訪問前驅），把下一個節點的值複製過來，再刪下一個。",
    ],
    ("linked-list", "遍歷"): [
        "遍歷時注意 null 判斷；用 curr = curr.next 推進，循環條件是 curr != null。",
        "如需在遍歷中刪除節點，保留 prev 指針，或使用 dummy head 統一邊界。",
    ],
    ("linked-list",): [
        "鏈表操作核心：先用 dummy head 統一頭節點特殊情況，再確認指針的更新順序（先斷再連）。",
        "分清楚改 prev.next 和改 curr.next 的時機，避免斷鏈。",
    ],

    # ── segment-tree-bit ─────────────────────────────────────────────────────
    ("segment-tree-bit", "樹狀陣列"): [
        "BIT（Fenwick Tree）：update(i, delta) 和 query(i)（前綴和）都是 O(log n)；update 時 i += lowbit(i)，query 時 i -= lowbit(i)。",
        "支援差分 BIT 實現區間加法點查詢：對 [l, r] 加 v，等同 diff.update(l, v); diff.update(r+1, -v)。",
    ],
    ("segment-tree-bit", "線段樹"): [
        "線段樹把 [1, n] 遞迴分成左右子樹，每個節點維護對應區間的統計值（和、最大值等）。",
        "push_down 在需要更新子節點時下傳 lazy tag；push_up 在更新完子節點後更新父節點。",
    ],
    ("segment-tree-bit", "Lazy"): [
        "Lazy 線段樹：區間更新時先在根打 tag 而不下推；查詢或再次更新子節點前才 push_down。",
        "tag 的合成要正確（先子再父的覆蓋順序），且 push_down 後清零父節點的 tag。",
    ],
    ("segment-tree-bit", "動態開點"): [
        "動態開點線段樹：不預先建立所有節點，只在訪問到時才動態分配；節點用鏈接而非陣列索引。",
        "適合值域很大（如 10^9）但更新次數少的情況；空間 O(q log V) 而非 O(V)。",
    ],
    ("segment-tree-bit", "逆序對"): [
        "BIT 計逆序對：從左到右掃描，對每個 a[i] 查詢已插入中比 a[i] 大的元素數量，加到答案。",
        "值域壓縮（離散化）把元素映射到 [1, n] 後再用 BIT。",
    ],
    ("segment-tree-bit",): [
        "點更新區間查詢用 BIT（O(log n) 操作）；需要區間更新時考慮 Lazy 線段樹。",
        "BIT 的 lowbit(i) = i & (-i)，是 i 的最低位 1 所代表的值。",
    ],

    # ── dsu ──────────────────────────────────────────────────────────────────
    ("dsu", "基礎"): [
        "find 做路徑壓縮（parent[x] = find(parent[x])），unite 按大小合併（小樹掛大樹），均攤 O(α(n))。",
        "用並查集偵測環：若合併時兩節點已在同一集合（find(a) == find(b)），加入這條邊會形成環。",
    ],
    ("dsu", "帶權"): [
        "帶權並查集：rel[x] 記錄 x 到根的偏移；find 壓縮路徑時，rel[x] += rel[parent[x]] 更新累積偏移。",
        "unite(a, b, w) 時，確保 rel[root_b] - rel[root_a] = w（即 b - a = w）。",
    ],
    ("dsu", "GCD"): [
        "用節點代表 GCD 類別：若 gcd(a, b) = g，把 a 和 b 合併到同一集合，代表元記為某公因數關係。",
        "GCD 並查集常用於「同為某 GCD 的倍數」之等價類問題。",
    ],
    ("dsu", "陣列"): [
        "陣列上的並查集：把陣列位置當節點，用並查集合併「相同值」或「相連的連續段」。",
        "常見用法：找下一個未被訪問的位置，路徑壓縮讓 find 跳過已訪問位置。",
    ],
    ("dsu",): [
        "find 做路徑壓縮，unite 按秩合併，保持整體樹高為 O(log n)；兩者合用均攤 O(α(n))。",
        "並查集只支援合併，不支援拆分；若需撤銷，使用不帶路徑壓縮的帶回滾版本。",
    ],

    # ── tree-dp ──────────────────────────────────────────────────────────────
    ("tree-dp", "換根"): [
        "換根 DP：先從任意根做一次 DFS 算「以這為根的子樹 DP 值」，再做第二次 DFS 把根換成每個節點更新答案。",
        "第二次 DFS：進入子節點 v 時，把 v 從父節點的貢獻中去除，重新計算以 v 為根時父側的值。",
    ],
    ("tree-dp", "路徑"): [
        "樹上路徑 DP：以每個節點 u 為路徑最高點，枚舉左右子樹各自延伸的最長路徑，更新跨 u 的最大路徑。",
        "維護每個節點的「最深延伸長度」，在 DFS 後序處理時合併左右子結果。",
    ],
    ("tree-dp", "背包"): [
        "樹形背包：把子樹的 DP 陣列看作背包，合併時枚舉當前子樹分配的容量，複雜度 O(n²)。",
        "注意合併順序：每次只把一棵子樹合入當前已合併的背包，避免同一子樹用兩次。",
    ],
    ("tree-dp", "獨立集"): [
        "樹的最大獨立集 DP：dp[v][0] = v 不選時子樹的最大，dp[v][1] = v 選時子樹的最大。",
        "轉移：dp[v][0] = Σ max(dp[c][0], dp[c][1])，dp[v][1] = Σ dp[c][0]。",
    ],
    ("tree-dp",): [
        "樹形 DP 通常以後序遍歷為枚舉順序：先遞迴子節點，回溯時合併子節點的 DP 值到當前節點。",
        "定義 dp[v] 時明確語意（以 v 為根的子樹，還是整棵樹且 v 是最高點），避免混淆。",
    ],

    # ── binary-tree ──────────────────────────────────────────────────────────
    ("binary-tree", "自底向上 DFS"): [
        "後序遍歷：先遞迴左右子樹，回來時合併子樹的計算結果（如高度、路徑長度、是否平衡）。",
        "常見返回值設計：返回 (height, is_balanced) 或 (-1 表示不平衡)，一次 DFS 完成所有判斷。",
    ],
    ("binary-tree", "自頂向下 DFS"): [
        "前序遍歷：先處理當前節點，再把參數（如路徑和、目標值）傳給子節點遞迴。",
        "適合需要從根傳資訊下去的問題，如路徑總和、驗證 BST 的範圍。",
    ],
    ("binary-tree", "二叉搜索樹"): [
        "BST 的中序遍歷結果是嚴格遞增序列；驗證 BST 時傳遞 (min, max) 範圍限制。",
        "查找/插入/刪除都沿著「比根小走左，比根大走右」的路徑，平均 O(log n)，最壞 O(n)。",
    ],
    ("binary-tree", "遍歷"): [
        "前序 DLR / 中序 LDR / 後序 LRD；迭代版用棧模擬，右節點先入棧（前序）或使用兩棧（後序）。",
        "BFS 遍歷（層序）：用佇列，每層入隊所有子節點，記錄每層的節點數以區分層次。",
    ],
    ("binary-tree", "插入/刪除"): [
        "BST 插入：遞迴找到 null 位置插入；刪除：找到目標後分三情況（無子、一子、兩子）處理。",
        "刪除有兩子的節點：找中序後繼（右子樹最左）或中序前驅，用其值替換後刪除那個更簡單的節點。",
    ],
    ("binary-tree", "建立"): [
        "從前序+中序或後序+中序重建樹：中序的根節點下標把序列分成左右子樹。",
        "用雜湊表預處理中序位置，避免 O(n) 線性搜尋，使整體複雜度降到 O(n)。",
    ],
    ("binary-tree", "最近公共祖先"): [
        "LCA 遞迴：若 p, q 分別在左右子樹（或其中一個是當前根），當前節點即為 LCA。",
        "倍增 LCA（大量查詢）：DFS 記錄深度，用 sparse table 跳 2^k 祖先，查詢 O(log n)。",
    ],
    ("binary-tree",): [
        "樹的問題通常從葉子往根（後序）或從根往葉子（前序）設計遞迴，確認哪個方向更自然。",
        "DFS 遞迴終止條件：node == null 時返回合適的初始值（0、true 或 INF）。",
    ],

    # ── binary-lifting-lca ───────────────────────────────────────────────────
    ("binary-lifting-lca",): [
        "倍增預處理：up[v][k] = v 的 2^k 祖先，DFS 時 up[v][0] = parent[v]，up[v][k] = up[up[v][k-1]][k-1]。",
        "查詢 LCA(u, v)：先把深的那個提到同樣深度，再一起從高位往低位跳，使兩者跳到 LCA 下方一步，再取父節點。",
        "若同時需要路徑上的最小/最大邊權，在倍增表中同步維護 min/max 值。",
    ],

    # ── computational-geometry ───────────────────────────────────────────────
    ("computational-geometry", "點、線"): [
        "叉積 cross(AB, AC) = (B-A) × (C-A) > 0 表示 C 在 AB 左側；= 0 共線；< 0 右側。",
        "點到直線距離 = |cross(AB, AP)| / |AB|；線段相交需同時判斷叉積符號和端點情況。",
    ],
    ("computational-geometry", "凸包"): [
        "Graham 掃描或 Andrew 單調鏈求凸包；先按 x 排序，分別掃上下凸殼，逆時針叉積 ≤ 0 時彈出。",
        "最終上下凸殼合併（去除重複端點）即為凸包；注意共線點的處理（保留或去除）依題意。",
    ],
    ("computational-geometry", "矩形"): [
        "給定對角頂點，其他兩頂點可以旋轉90度得到；面積 = |對角線長度|² / 2。",
        "判斷點在矩形內：用叉積判斷點在四條邊的內側，或用 AABB 快速篩選後精確判斷。",
    ],
    ("computational-geometry", "圓"): [
        "圓上任意點 = center + r*(cos θ, sin θ)；兩圓交點需聯立方程，用餘弦定理算角度。",
        "圓的掃描線：把每個圓的進入/離開角度排序，掃描計算最大重疊數。",
    ],
    ("computational-geometry", "曼哈頓"): [
        "曼哈頓距離轉切比雪夫：設 u = x+y, v = x-y，則 |x1-x2| + |y1-y2| = max(|u1-u2|, |v1-v2|)。",
        "最小曼哈頓距離生成樹：轉成切比雪夫後建坐標排序再用 BIT 加速建圖，或直接用平面 Kruskal。",
    ],
    ("computational-geometry",): [
        "叉積用於判斷方向和面積；點積用於判斷垂直和投影長度。",
        "浮點精度：除非題目保證整數，否則用 eps = 1e-9 做比較，避免等號直接比較浮點數。",
    ],

    # ── minimum-spanning-tree ────────────────────────────────────────────────
    ("minimum-spanning-tree",): [
        "Kruskal：邊按權重排序，用並查集判斷加入邊是否形成環，選 n-1 條不形成環的最小邊。",
        "Prim：從任意節點開始，用最小堆貪心擴展，每次選代價最小的非樹邊加入 MST。",
        "稠密圖（邊數 >> 節點數）Prim 更快（O(n²)），稀疏圖 Kruskal 更快（O(E log E)）。",
    ],

    # ── network-flow ─────────────────────────────────────────────────────────
    ("network-flow",): [
        "最大流（Max-Flow）：Dinic 演算法，先 BFS 建層次圖，再 DFS 多路增廣，複雜度 O(V² E)。",
        "最小割 = 最大流（Max-flow min-cut 定理）；費用流（Min-Cost Max-Flow）在 Dinic 的基礎上改用 SPFA/Dijkstra 找增廣路。",
        "二分圖最大匹配 = 最大流（源 → 左部節點 → 右部節點 → 匯，邊容量均為 1）。",
    ],

    # ── string-algorithms ────────────────────────────────────────────────────
    ("string-algorithms", "KMP"): [
        "KMP 用 fail 陣列（次長相同前後綴長度）加速匹配，避免主串指針回退；fail 的計算本身是一次 KMP 自匹配。",
        "fail[0] = -1, fail[1] = 0；計算 fail[i] 時從 fail[i-1] 開始跳，直到匹配或到 -1。",
    ],
    ("string-algorithms", "Z 函式"): [
        "Z[i] = s[i..] 與 s[0..] 的最長公共前綴長度；可用 Z-box 線性計算：維護 [l, r] 為目前最遠的 z-box 右界。",
        "Z 函式可代替 KMP：把模式拼接 '#' 拼目標串，z[i] == |pattern| 即在位置 i-len-1 找到匹配。",
    ],
    ("string-algorithms", "字典樹"): [
        "Trie（字典樹）把字串按字元逐層插入，每個節點代表前綴；查詢前綴存在性或統計字串個數 O(L)。",
        "AC 自動機 = Trie + KMP 的失敗指針，支援多模式串同時匹配；fail 指向最長可匹配後綴的節點。",
    ],
    ("string-algorithms", "後綴陣列"): [
        "後綴陣列 SA[i] = 排名第 i 的後綴起始位置；LCP 陣列 lcp[i] = SA[i] 和 SA[i-1] 的最長公共前綴。",
        "SA-IS 或 DC3 算法 O(n) 建後綴陣列；LCP 陣列配合稀疏表可 O(1) 查詢任意後綴對的 LCP。",
    ],
    ("string-algorithms", "雜湊"): [
        "字串雜湊：hash(s[l..r]) = Σ s[i] × base^(r-i) mod p；區間雜湊 = (hash(s[0..r]) - hash(s[0..l-1]) × base^(r-l+1)) mod p。",
        "選多個質數基底（雙雜湊）降低碰撞機率；注意負數取模要加 p。",
    ],
    ("string-algorithms", "回文"): [
        "Manacher 演算法 O(n) 求所有回文子串長度：維護最遠回文中心 c 和右界 r，利用鏡像初始化減少重複計算。",
        "將奇偶回文統一：在字元間插入特殊字元（如 #），使所有回文都是奇數長度。",
    ],
    ("string-algorithms", "表示式"): [
        "遞迴下降解析運算式：expression → term { +/- term }，term → factor { */÷ factor }，factor → num | (expression)。",
        "雙棧法（值棧 + 運算符棧）：遇低優先級運算符時先彈出高優先級並計算，再推入新運算符。",
    ],
    ("string-algorithms", "優化 DP"): [
        "字典樹優化 DP：把字串的前綴/後綴插入 Trie，DP 轉移時在 Trie 上同步走，避免枚舉所有分割點。",
        "AC 自動機優化 DP：在 AC 自動機的狀態圖上做 DP，每個狀態是目前匹配到的後綴。",
    ],
    ("string-algorithms", "LCP 陣列"): [
        "後綴陣列 + LCP 陣列：任意後綴對的 LCP = 它們在 SA 中對應區間的最小 lcp 值，用稀疏表 O(1) 查詢。",
        "Z 函式與 KMP fail 的關係：fail[i] 是 s[0..i-1] 的最長相同前後綴長度，Z[i] 是 s[i..] 與 s 的 LCP。",
    ],
    ("string-algorithms", "括號"): [
        "棧匹配括號：左括號入棧，右括號與棧頂匹配則彈出，否則不合法；最後棧為空才是完全合法。",
        "最長合法括號子串：用 DP 或棧記錄每個位置的最長合法長度，掃描維護棧底的未匹配位置。",
    ],
    ("string-algorithms",): [
        "先識別字串問題的類型（匹配、最長公共子串、回文、分割），再選 KMP/Z/Manacher/雜湊。",
        "字串 DP 通常以字元位置為狀態，注意子字串索引的 [l, r] 閉或 [l, r) 半開定義要統一。",
    ],
}

# Template hint patterns to replace
TEMPLATE_PATTERNS = [
    "lc-rating「",
    "lc-rating 競賽題庫題目",
    "先辨認本節模型再動手寫模板",
    "可作為練習場隨機抽題",
    "rating 約",
]


def is_template_hint(hints: list[str]) -> bool:
    for h in hints:
        if any(p in h for p in TEMPLATE_PATTERNS):
            return True
    return False


def find_hints(topic_id: str, section: str | None) -> list[str] | None:
    """Look up hints from the mapping, trying specific then general keys."""
    if section:
        # Try exact match first
        if (topic_id, section) in HINTS:
            return HINTS[(topic_id, section)]
        # Try substring match
        for key_section in [k[1] for k in HINTS if len(k) == 2 and k[0] == topic_id]:
            if key_section and key_section in section:
                return HINTS[(topic_id, key_section)]
    # Fallback to topic-only
    if (topic_id,) in HINTS:
        return HINTS[(topic_id,)]
    return None


def main():
    with open("data/problems.json", encoding="utf-8") as f:
        problems = json.load(f)

    replaced = 0
    no_mapping = set()

    for problem in problems:
        hints = problem.get("strategy_hints", [])
        if not is_template_hint(hints):
            continue  # Keep good hints as-is

        topic_id = problem.get("topic_id", "")
        refs = problem.get("study_plan_refs", [])
        section = refs[0].get("section_title", "") if refs else ""

        # Clean section: strip leading ". " markers used in section names
        section_clean = section.lstrip(". ").strip() if section else ""

        new_hints = find_hints(topic_id, section_clean)
        if new_hints is None:
            new_hints = find_hints(topic_id, None)

        if new_hints:
            problem["strategy_hints"] = new_hints
            replaced += 1
        else:
            no_mapping.add((topic_id, section_clean))

    print(f"Replaced hints for {replaced} problems.")
    if no_mapping:
        print(f"No mapping found for {len(no_mapping)} (topic, section) combos:")
        for combo in sorted(no_mapping):
            print(f"  {combo}")

    with open("data/problems.json", "w", encoding="utf-8") as f:
        json.dump(problems, f, ensure_ascii=False, indent=2)
    print("data/problems.json updated.")


if __name__ == "__main__":
    main()
